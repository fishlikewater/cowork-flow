{
  "name": "cowork-flow",
  "version": "0.0.45",
  "description": "CLI for installing and maintaining cowork-flow collaboration templates.",
  "type": "module",
  "bin": {
    "cowork-flow": "./bin/cowork-flow.js"
  },
  "files": [
    "bin",
    "src",
    "template",
    "template/.zcode",
    "README.md",
    "!template/**/__pycache__/**",
    "!template/**/*.pyc"
  ],
  "scripts": {
    "test": "npm run test:fast",
    "test:fast": "node --test test/asset-plan.test.js test/cli.test.js test/host-assets.test.js test/opencode-plugin.test.js test/template-runner.test.js test/update.test.js test/zcode-plugin.test.js",
    "test:integration": "node --test --test-name-pattern=\"^(init copies the template into a new target directory|sync updates safe template files and preserves protected files)$\" test/init.test.js test/sync.test.js",
    "test:node:full": "node --test",
    "test:template": "node scripts/run-template-tests.js --suite core",
    "test:template:full": "node scripts/run-template-tests.js --suite full",
    "pack:check": "node scripts/pack-check.js",
    "test:all": "npm run test:node:full && npm run test:template:full && npm run pack:check",
    "release": "sh scripts/release.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
